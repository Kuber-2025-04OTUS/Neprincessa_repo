apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hw-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hw-app.selectorLabels" . | nindent 4 }}
